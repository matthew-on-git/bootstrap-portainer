---
title: 'Bootstrap Portainer CE Installation Script (v1)'
type: 'feature'
created: '2026-05-05'
status: 'superseded'
context: []
baseline_commit: '547eafc963d67cc0b95aafc4c9b38523d16884ff'
superseded_by: 'spec-install-script-v2.md'
---

> **⚠ Superseded — historical record only.** This v1 contract assumed Docker was pre-installed. The product intent has been formally renegotiated; the authoritative spec is now [`spec-install-script-v2.md`](./spec-install-script-v2.md). This file is preserved unchanged below the line for traceability.

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** There is no idempotent, re-runnable installation script for deploying Portainer CE via Docker Compose on Ubuntu servers, following the established patterns from bootstrap-supabase.

**Approach:** Create an `install.sh` script that deploys Portainer CE using Docker Compose with pinned image versions, interactive configuration prompts, and support for both interactive and unattended (-y flag) modes.

## Boundaries & Constraints

**Always:**
- Follow bootstrap-supabase patterns (idempotent, re-runnable, pinned versions)
- Use Docker Compose (not docker run) for deployment
- Support Ubuntu 22.04/24.04
- Use `portainer/portainer-ce:sts` image (pinned version)
- Expose ports 9443 (HTTPS UI) and 8000 (Edge tunnel)
- Mount Docker socket and persistent volume
- Save configuration to `.install.conf` for re-runs
- Use shared logging library (log_info, log_warn, log_error, die from lib/log.sh)

**Ask First:**
- Changes to default ports (9443, 8000)
- Adding TLS/HTTPS configuration
- Adding SMTP or authentication settings
- Supporting additional Portainer Agent deployments

**Never:**
- Use `:latest` tags for images
- Break idempotency (must be safe to re-run)
- Install Docker (assume it's already installed)
- Support non-Ubuntu distributions
- Add Portainer Business Edition features

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Fresh install | First run, no existing config | Creates `.install.conf`, deploys Portainer via docker compose, container running | Die if Docker unavailable |
| Re-run existing | Config exists, container running | No-op (already running), reports status | Warn if config corrupted |
| Unattended mode | `-y` flag passed | Uses all defaults, no prompts | Die on missing required values |
| Custom port | User specifies non-default port | Uses specified port in compose file and container | Validate port is available |
| Docker unavailable | Docker not installed/running | Script fails with clear error | Die with message about Docker requirement |

</frozen-after-approval>

## Code Map

- `install.sh` -- Main idempotent installation script (follows bootstrap-supabase patterns)
- `lib/log.sh` -- Shared logging library (log_info, log_warn, log_error, die functions)
- `docker-compose.yaml` -- Portainer CE compose file (generated by install.sh)
- `.install.conf` -- Configuration file (created on first run, reloaded on re-runs)

## Tasks & Acceptance

**Execution:**
- [x] `install.sh` -- Create main installation script following bootstrap-supabase patterns -- Provide idempotent Portainer CE deployment with interactive/unattended modes
- [x] `lib/log.sh` -- Create shared logging library -- Reuse logging patterns from bootstrap-supabase for consistency
- [x] `docker-compose.yaml` (generated) -- Generate Docker Compose file with pinned Portainer image -- Use `portainer/portainer-ce:sts` with proper volumes, ports, and restart policy

**Acceptance Criteria:**
- Given fresh Ubuntu 22.04/24.04 system with Docker installed, when `sudo bash install.sh` is run, then Portainer CE container is deployed and accessible on port 9443
- Given existing `.install.conf` and running Portainer container, when `install.sh` is re-run, then script reports "already running" and exits without changes
- Given `-y` flag is passed, when `install.sh` is run, then all defaults are accepted without prompting
- Given Docker is not installed, when `install.sh` is run, then script dies with clear error message
- Given `make check` is run, then all linting, formatting, and security checks pass

## Spec Change Log

1. **Loop 1 (patch)** — Review found missing error handling and validation:
   - Added `docker compose version` pre-flight check (docker compose plugin required)
   - Added `valid_port()` and `prompt_port()` functions for port validation (1-65535)
   - Added corrupted config handling (`source` failure caught, falls back to defaults)
   - Added `mkdir -p` and `chmod` failure checks with `die`
   - Added compose file write success check (`cat >` failure caught)
   - Added deploy error handling (`docker compose up -d` failure caught with message)
   - Added retry loop (5×2s) for container readiness check
   - Added `SCRIPT_DIR` resolution failure check
   - **KEEP**: Idempotency logic, interactive/unattended modes, bootstrap-supabase patterns, shared logging library

## Design Notes

**Compose File Template:**
```yaml
services:
  portainer:
    container_name: portainer
    image: portainer/portainer-ce:sts
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    ports:
      - 9443:9443
      - 8000:8000

volumes:
  portainer_data:
    name: portainer_data

networks:
  default:
    name: portainer_network
```

**Key Differences from bootstrap-supabase:**
- Single container (not multi-service stack)
- No database to configure
- No SMTP/auth complexity
- Simpler overall (good first project pattern)

## Verification

**Commands:**
- `docker ps` -- expected: `portainer` container running with ports 9443 and 8000 exposed
- `curl -k https://localhost:9443` -- expected: Portainer login page response
- `sudo bash install.sh -y && sudo bash install.sh -y` -- expected: second run reports already running (idempotent)
- `make check` -- expected: all linting and security checks pass

## Suggested Review Order

Read [`install.sh`](../../install.sh) top-to-bottom in these sections (line numbers drift; headings are authoritative):

**Entry point and core logic** — Banner comment, **`set -euo pipefail`**, constants, **`# Load Shared Library`** (bootstrap `SCRIPT_DIR` + **`source`** `lib/log.sh` via `printf`/exit-safe path).

**Validation and error handling** — **`ensure_ubuntu_supported`**, **`install_docker_engine_ubuntu`**, **`ensure_docker_daemon_and_compose`**, **`add_sudo_invoker_to_docker_group`**, registry probe (`log_warn`). **`valid_port`**, **`tcp_port_listening`**, **`ensure_publish_ports_free_for_compose_deploy`**.

**Configuration management** — **Load Saved Config**, **Interactive Configuration** (`prompt` / `prompt_port`), **Save Configuration** (`cat` HEREDOC `.install.conf`).

**Deployment** — **Idempotency Check** (running / stopped / fresh path), **`COMPOSE_FILE` HEREDOC**, **`docker compose up -d`**, readiness polling.

**Shared library** — [`lib/log.sh`](../../lib/log.sh) (`log_*`, **`die`**, **`banner`**).

### Review Findings

- [x] [Review][Patch] `die` called before `lib/log.sh` defines it — **Resolved:** bootstrap uses `printf`/exit until `lib/log.sh` is sourced [`install.sh` — `# Load Shared Library`](../../install.sh)
- [x] [Review][Patch] Host port availability — **Resolved:** **`ensure_publish_ports_free_for_compose_deploy`** + **`ss`** on fresh deploy branch [`install.sh` — `# Interactive Configuration` / `# Idempotency Check`](../../install.sh)
- [x] [Review][Patch] Registry reachability probe — **Resolved:** **`log_warn`** for hub.docker.com; non-fatal [`install.sh` — `# Pre-flight Checks`](../../install.sh)
